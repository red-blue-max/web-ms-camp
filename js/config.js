/* =========================================================
   НАСТРОЙКИ САЙТА — единственный файл, который нужно править
   при переезде на другой хостинг / подключении Метрики.
   ========================================================= */
window.SITE_CONFIG = {
  /* ---------- Яндекс.Метрика ----------
     Вставьте номер счётчика (только цифры), например '98765432'.
     Пока стоит плейсхолдер — Метрика не загружается. */
  METRIKA_ID: '[[METRIKA_ID]]',

  /* ---------- Отправка заявок в Telegram ----------
     'proxy'  — ОСНОВНОЙ режим. Форма шлёт данные на серверный скрипт
                (api/send.php на хостинге Timeweb), токен бота хранится
                на сервере и в браузер не попадает.
     'direct' — ВРЕМЕННЫЙ MVP-режим: браузер сам шлёт запрос в api.telegram.org.
                ⚠️ ТОКЕН БОТА БУДЕТ ВИДЕН ЛЮБОМУ в devtools / исходном коде.
                Любой сможет слать сообщения от имени бота. Используйте только
                пока нет бэкенда и сразу после переезда на 'proxy' перевыпустите
                токен в @BotFather (/revoke).                                  */
  FORM_MODE: 'proxy',

  // Адрес прокси. Относительный путь работает на любом хостинге с PHP.
  // Для Cloudflare Worker / другой функции — укажите полный URL.
  PROXY_URL: 'api/send.php',

  // Только для FORM_MODE: 'direct' (см. предупреждение выше!)
  DIRECT_BOT_TOKEN: '',
  DIRECT_CHAT_ID: '',
};
